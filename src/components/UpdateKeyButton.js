import '../assets/css/components/update-key-button.css'

export const UpdateKeyButton = ({ margin }) => {
    return (
        <div style={{margin: margin}} className="update-key-btn">
            <span>+</span>
        </div>
    )
}